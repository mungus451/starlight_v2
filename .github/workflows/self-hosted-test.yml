name: Self-Hosted Runner Test

on:
  pull_request:
    branches: [ master ]

permissions:
  contents: read

jobs:
  connectivity-test:
    name: Test Self-Hosted Runner Connectivity
    runs-on: self-hosted

    steps:
      - name: Echo Hello World
        run: echo "Hello World from self-hosted runner!"
