name: Tests & Architecture Audits

on:
  push:
    branches: [ master, dev, mungus/EliteUnits ]
  pull_request:
    branches: [ master, dev, mungus/EliteUnits ]

jobs:
  tests:
    name: PHPUnit Tests & Architecture Verification shr
    runs-on: self-hosted

    services:
      mysql:
        image: mariadb:11
        env:
          MARIADB_ROOT_PASSWORD: rootpass
          MARIADB_DATABASE: starlightDB
          MARIADB_USER: sd_admin
          MARIADB_PASSWORD: starlight
        ports:
          - 33306:3306
        options: >-
          --health-cmd="healthcheck.sh --connect --innodb_initialized"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

      redis:
        image: redis:7-alpine
        ports:
          - 6380:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.5'
          extensions: mbstring, pdo, pdo_mysql, redis
          coverage: xdebug

      - name: Get Composer cache directory
        id: composer-cache
        run: echo "dir=$(composer config cache-files-dir)" >> $GITHUB_OUTPUT

      - name: Cache Composer dependencies
        uses: actions/cache@v4
        with:
          path: ${{ steps.composer-cache.outputs.dir }}
          key: ${{ runner.os }}-composer-${{ hashFiles('**/composer.lock') }}
          restore-keys: ${{ runner.os }}-composer-

      - name: Install dependencies
        run: composer install --prefer-dist --no-progress --no-interaction

      - name: Setup environment file
        run: |
          cp .env.example .env
          sed -i 's/DB_HOST=db/DB_HOST=127.0.0.1/' .env
          sed -i 's/DB_PORT=3306/DB_PORT=33306/' .env
          sed -i 's/REDIS_HOST=redis/REDIS_HOST=127.0.0.1/' .env
          sed -i 's/REDIS_PORT=6379/REDIS_PORT=6380/' .env
          sed -i 's/APP_ENV=development/APP_ENV=testing/' .env
          sed -i 's/# DB_DATABASE_TEST=starlightDB_test/DB_DATABASE_TEST=starlightDB_test/' .env

      - name: Wait for MySQL
        run: |
          for i in {1..30}; do
            if mysqladmin ping -h 127.0.0.1 -P 33306 --silent; then
              echo "MySQL is ready"
              break
            fi
            echo "Waiting for MySQL... ($i/30)"
            sleep 2
          done

      - name: Create test database
        run: |
          MYSQL_PWD=rootpass mysql -h 127.0.0.1 -P 33306 -u root -e "CREATE DATABASE IF NOT EXISTS starlightDB_test;"
          MYSQL_PWD=rootpass mysql -h 127.0.0.1 -P 33306 -u root -e "GRANT ALL PRIVILEGES ON starlightDB_test.* TO 'sd_admin'@'%'; FLUSH PRIVILEGES;"

      - name: Run Phinx migrations for test database
        run: vendor/bin/phinx migrate -e testing --configuration=config/phinx.php

      - name: Run MVC Compliance Audit
        run: php tests/Compliance/mvc_lint.php

      - name: Run Session Decoupling Audit
        run: php tests/Compliance/VerifySessionDecoupling.php

      - name: Run DI Resolution Verification
        run: php tests/Compliance/verify_di_resolution.php

      - name: Run PHPUnit Tests with Coverage
        run: vendor/bin/phpunit --coverage-text --coverage-clover=coverage.xml --colors=never

      - name: Check Coverage Threshold
        run: |
          COVERAGE=$(php -r "
            \$xml = simplexml_load_file('coverage.xml');
            \$metrics = \$xml->project->metrics;
            \$lines = (int)\$metrics['coveredstatements'];
            \$total = (int)\$metrics['statements'];
            if (\$total > 0) {
              echo round((\$lines / \$total) * 100, 2);
            } else {
              echo '0';
            }
          ")
          echo "Code Coverage: ${COVERAGE}%"
          # Coverage threshold is intentionally set low (10%) for initial project setup.
          # As test coverage improves, plan to incrementally raise this threshold.
          # Documenting rationale for future maintainers.
          THRESHOLD=20
          if (( $(php -r "echo ($COVERAGE < $THRESHOLD) ? 1 : 0;") )); then
            echo "❌ Coverage ${COVERAGE}% is below required ${THRESHOLD}%"
            exit 1
          else
            echo "✅ Coverage ${COVERAGE}% meets ${THRESHOLD}% threshold"
          fi

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-report
          path: coverage.xml